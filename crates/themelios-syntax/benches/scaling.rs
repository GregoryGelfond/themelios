//! Placeholder for the out-of-band scaling benchmarks
//! (docs/design/syntax.md §16). The bench target is declared in the
//! manifest before the criterion harness that will fill it, so the
//! target's identity is fixed from the crate's first commit; this file
//! is replaced by that harness once the surfaces it measures exist.
fn main() {}
